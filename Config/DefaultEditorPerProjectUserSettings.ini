[/Script/ModelContextProtocolEngine.ModelContextProtocolSettings]
; Start the built-in Epic MCP server (http://127.0.0.1:8000/mcp) automatically when the editor launches,
; so agent clients (.mcp.json -> "unreal-mcp") can connect without a manual command.
bAutoStartServer=True
ServerPortNumber=8000
ServerUrlPath=/mcp
bEnableToolSearch=True
